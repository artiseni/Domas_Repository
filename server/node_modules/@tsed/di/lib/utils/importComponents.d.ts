export declare function mapConfiguration(config: any): {
    endpoint?: string;
    values: any[];
}[];
export declare function importComponents(config: any, excludes: string[]): Promise<{
    token: string;
    route?: string;
}[]>;
