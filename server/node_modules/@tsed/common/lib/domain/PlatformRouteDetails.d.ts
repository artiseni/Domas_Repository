import { EndpointMetadata } from "./EndpointMetadata";
import { ControllerProvider } from "./ControllerProvider";
export interface PlatformRouterDetailsOptions {
    provider: ControllerProvider;
    endpoint: EndpointMetadata;
    method: string;
    url: string;
}
export declare class PlatformRouteDetails {
    readonly method: string;
    readonly url: string;
    readonly rawBody: boolean;
    readonly endpoint: EndpointMetadata;
    readonly provider: ControllerProvider;
    constructor({ provider, endpoint, method, url }: PlatformRouterDetailsOptions);
    get name(): string;
    get className(): string;
    get methodClassName(): string;
    get parameters(): import("@tsed/platform-params").ParamMetadata[];
    toJSON(): {
        method: string;
        name: string;
        url: string;
        className: string;
        methodClassName: string;
        parameters: import("@tsed/platform-params").ParamMetadata[];
        rawBody: boolean;
    };
}
