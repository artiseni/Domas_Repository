import { InjectorService } from "@tsed/di";
import { Platform } from "./services/Platform";
/**
 * @ignore
 */
export declare class PlatformModule {
    protected injector: InjectorService;
    protected platform: Platform;
    constructor(injector: InjectorService, platform: Platform);
    /**
     * Create routers from the collected controllers
     */
    createRoutersFromControllers(): (import("./interfaces").PlatformRouterMethods<any> | undefined)[];
    $$loadRoutes(): void;
}
