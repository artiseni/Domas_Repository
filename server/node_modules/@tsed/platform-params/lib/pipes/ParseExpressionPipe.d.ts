import { PipeMethods, ParamMetadata } from "../domain/ParamMetadata";
import type { ArgScope } from "../builder/PlatformParams";
export declare class ParseExpressionPipe implements PipeMethods {
    transform(scope: ArgScope, param: ParamMetadata): any;
}
