import { isObject, prototypeOf, ancestorsOf, DecoratorTypes, getValue, nameOf, decoratorTypeOf, useDecorators, isString } from '@tsed/core';
import { __decorate, __metadata, __param } from 'tslib';
import { JsonEntityStore, JsonEntityComponent, getJsonSchema, isParameterType, Name, CollectionOf, Any } from '@tsed/schema';
import { Injectable, Configuration, InjectorService, Inject, ProviderScope } from '@tsed/di';
import { deserialize } from '@tsed/json-mapper';
import { BadRequest } from '@tsed/exceptions';

function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

var id = 0;

function _classPrivateFieldLooseKey(name) {
  return "__private_" + id++ + "_" + name;
}

function _classPrivateFieldLooseBase(receiver, privateKey) {
  if (!Object.prototype.hasOwnProperty.call(receiver, privateKey)) {
    throw new TypeError("attempted to use private field on non-instance");
  }

  return receiver;
}

/**
 * @ignore
 */

function mapParamsOptions(args) {
  if (args.length === 1) {
    if (typeof args[0] === "string") {
      return {
        expression: args[0]
      };
    }

    if (!isObject(args[0])) {
      return {
        useType: args[0]
      };
    }

    return _extends({}, args[0]);
  }

  return {
    expression: args[0],
    useType: args[1]
  };
}

var ParamTypes;

(function (ParamTypes) {
  ParamTypes["$CTX"] = "$CTX";
  ParamTypes["BODY"] = "BODY";
  ParamTypes["PATH"] = "PATH";
  ParamTypes["QUERY"] = "QUERY";
  ParamTypes["HEADER"] = "HEADER";
  ParamTypes["RAW_BODY"] = "RAW_BODY";
  ParamTypes["COOKIES"] = "COOKIES";
  ParamTypes["LOCALS"] = "LOCALS";
  ParamTypes["SESSION"] = "SESSION";
  ParamTypes["FILES"] = "FILES"; /// NOT STD

  ParamTypes["NEXT_FN"] = "NEXT_FN";
  ParamTypes["ERR"] = "ERR";
  ParamTypes["REQUEST"] = "REQUEST";
  ParamTypes["PLATFORM_REQUEST"] = "PLATFORM_REQUEST";
  ParamTypes["NODE_REQUEST"] = "NODE_REQUEST";
  ParamTypes["RESPONSE"] = "RESPONSE";
  ParamTypes["PLATFORM_RESPONSE"] = "PLATFORM_RESPONSE";
  ParamTypes["NODE_RESPONSE"] = "NODE_RESPONSE";
  ParamTypes["RESPONSE_DATA"] = "RESPONSE_DATA";
  ParamTypes["ENDPOINT_INFO"] = "ENDPOINT_INFO";
  ParamTypes["RES"] = "RES";
  ParamTypes["REQ"] = "REQ";
})(ParamTypes || (ParamTypes = {}));

let ParamMetadata = class ParamMetadata extends JsonEntityStore {
  constructor(options) {
    super(options);
    this.paramType = "$ctx";
    this.pipes = [];
    const {
      paramType,
      pipes,
      dataPath
    } = options;
    this.expression = options.expression || this.expression;
    this.paramType = paramType || this.paramType;
    this.dataPath = dataPath || this.dataPath;
    this.pipes = pipes || [];
  }

  get key() {
    let {
      expression,
      paramType,
      dataPath
    } = this;

    if (expression && paramType === ParamTypes.HEADER) {
      expression = String(expression).toLowerCase();
    }

    return [dataPath, expression].filter(Boolean).join(".");
  }
  /**
   * Return the JsonOperation
   */


  get parameter() {
    return this._parameter;
  }

  static get(target, propertyKey, index) {
    return JsonEntityStore.from(prototypeOf(target), propertyKey, index);
  }

  static getParams(target, propertyKey) {
    const params = [];
    const klass = ancestorsOf(target).reverse().find(target => JsonEntityStore.fromMethod(target, propertyKey).children.size);

    if (!klass) {
      return [];
    }

    JsonEntityStore.fromMethod(klass, propertyKey).children.forEach((param, index) => {
      params[+index] = param;
    });
    return params;
  }

};
ParamMetadata = __decorate([JsonEntityComponent(DecoratorTypes.PARAM), __metadata("design:paramtypes", [Object])], ParamMetadata);

let ParseExpressionPipe = class ParseExpressionPipe {
  transform(scope, param) {
    const {
      paramType,
      type
    } = param;
    const value = getValue(scope, param.key);

    if ([ParamTypes.QUERY, ParamTypes.PATH].includes(paramType) && value === "" && type !== String) {
      return undefined;
    }

    return value;
  }

};
ParseExpressionPipe = __decorate([Injectable({
  priority: -1000
})], ParseExpressionPipe);

var _settings;

var DeserializerPipe_1;
let DeserializerPipe = DeserializerPipe_1 = (_settings = /*#__PURE__*/_classPrivateFieldLooseKey("settings"), class DeserializerPipe {
  constructor(configuration) {
    Object.defineProperty(this, _settings, {
      writable: true,
      value: void 0
    });
    _classPrivateFieldLooseBase(this, _settings)[_settings] = configuration.get("jsonMapper", configuration.get("converter", {}));
  }

  transform(value, param) {
    return deserialize(value, _extends({
      useAlias: true,
      additionalProperties: _classPrivateFieldLooseBase(this, _settings)[_settings].additionalProperties === "accept",
      type: param.type,
      collectionType: param.collectionType,
      groups: param.parameter.groups,
      genericTypes: param.nestedGenerics[0],
      nestedGenerics: param.nestedGenerics
    }, param.store.get(DeserializerPipe_1) || {}));
  }

});
DeserializerPipe = DeserializerPipe_1 = __decorate([Injectable(), __param(0, Configuration()), __metadata("design:paramtypes", [Object])], DeserializerPipe);

class ValidationError extends BadRequest {
  constructor(message, errors = []) {
    super(message);
    this.name = "VALIDATION_ERROR";
    this.errors = errors;
  }

}

class RequiredValidationError extends ValidationError {
  constructor() {
    super(...arguments);
    this.name = "REQUIRED_VALIDATION_ERROR";
  }

  static from(metadata) {
    const name = nameOf(metadata.paramType);
    const expression = metadata.expression;
    const type = name.toLowerCase().replace(/parse|params|filter/gi, "");
    const message = `It should have required parameter '${expression}'`;
    const errors = [{
      dataPath: "",
      keyword: "required",
      message,
      modelName: type,
      params: {
        missingProperty: expression
      },
      schemaPath: "#/required"
    }];
    return new RequiredValidationError(message, errors);
  }

}

let ValidationPipe = class ValidationPipe {
  constructor(injector) {
    const provider = injector.getProviders().find(provider => nameOf(provider.token) === "AjvService");

    if (provider) {
      this.validator = injector.invoke(provider.token);
    }
  }

  coerceTypes(value, metadata) {
    if (value === undefined) {
      return value;
    }

    if (value === "null") {
      return null;
    }

    if (metadata.isPrimitive) {
      try {
        return deserialize(value, {
          type: metadata.type
        });
      } catch (er) {
        return value;
      }
    }

    if (metadata.isArray) {
      return [].concat(value);
    }

    return value;
  }

  skip(value, metadata) {
    return metadata.paramType === ParamTypes.PATH && !metadata.isPrimitive;
  }

  async transform(value, metadata) {
    if (!this.validator) {
      this.checkIsRequired(value, metadata);
      return value;
    }

    if (this.skip(value, metadata)) {
      return value;
    }

    value = this.coerceTypes(value, metadata);
    this.checkIsRequired(value, metadata);

    if (value === undefined) {
      return value;
    }

    const schema = getJsonSchema(metadata, {
      customKeys: true
    });
    await this.validator.validate(value, {
      schema,
      type: metadata.isClass ? metadata.type : undefined,
      collectionType: metadata.collectionType
    });
    return value;
  }

  checkIsRequired(value, metadata) {
    if (metadata.isRequired(value)) {
      throw RequiredValidationError.from(metadata);
    }

    return true;
  }

};
ValidationPipe = __decorate([Injectable({
  type: "validator"
}), __metadata("design:paramtypes", [InjectorService])], ValidationPipe);

class ParamValidationError extends BadRequest {
  constructor() {
    super(...arguments);
    this.name = "PARAM_VALIDATION_ERROR";
  }

  static from(metadata, origin = {}) {
    if (origin instanceof ValidationError || origin instanceof BadRequest) {
      const name = nameOf(metadata.paramType).toLowerCase().replace(/parse|params|filter/gi, "");
      const expression = metadata.expression;
      const message = `Bad request on parameter "request.${name}${expression ? "." + expression : ""}".\n${origin.message}`.trim();
      const error = new ParamValidationError(message);
      error.dataPath = String(metadata.expression) || "";
      error.requestType = nameOf(metadata.paramType);
      error.origin = origin.origin || origin;
      return error;
    }

    return origin;
  }

}

var _caches;
/**
 * Platform Params abstraction layer.
 * @platform
 */

let PlatformParams = (_caches = /*#__PURE__*/_classPrivateFieldLooseKey("caches"), class PlatformParams {
  constructor() {
    Object.defineProperty(this, _caches, {
      writable: true,
      value: void 0
    });
    _classPrivateFieldLooseBase(this, _caches)[_caches] = new WeakMap();
  }

  async build(param) {
    const get = pipe => {
      return this.injector.getProvider(pipe).priority || 0;
    };

    const sort = (p1, p2) => get(p1) < get(p2) ? -1 : get(p1) > get(p2) ? 1 : 0;

    const map = token => this.injector.invoke(token);

    const promises = await Promise.all([ParseExpressionPipe, ...param.pipes.sort(sort)].map(map));
    const pipes = promises.filter(Boolean);

    _classPrivateFieldLooseBase(this, _caches)[_caches].set(param, pipes);

    return pipes;
  }

  getPipes(param) {
    return _classPrivateFieldLooseBase(this, _caches)[_caches].get(param) || [];
  }

  async runPipes(scope, param) {
    return this.getPipes(param).reduce(async function (value, pipe) {
      value = await value;

      try {
        return await pipe.transform(value, param);
      } catch (er) {
        throw ParamValidationError.from(param, er);
      }
    }, scope);
  }
  /**
   * Return arguments to call handler
   */


  async getArgs(scope, parameters) {
    return Promise.all(parameters.map(param => this.runPipes(scope, param)));
  }

});

__decorate([Inject(), __metadata("design:type", InjectorService)], PlatformParams.prototype, "injector", void 0);

PlatformParams = __decorate([Injectable({
  scope: ProviderScope.SINGLETON
})], PlatformParams);

/**
 * Get the Param metadata. Use this decorator to compose your own decorator.
 *
 * @param fn
 * @decorator
 * @operation
 * @input
 */

function ParamFn(fn) {
  return (target, propertyKey, index) => {
    if (decoratorTypeOf([target, propertyKey, index]) === DecoratorTypes.PARAM) {
      fn(ParamMetadata.get(target, propertyKey, index), [target, propertyKey, index]);
    }
  };
}

/**
 * Register a Pipe to be used with the current decorated param.
 *
 * @param token
 * @param options
 * @decorator
 * @operation
 * @input
 * @pipe
 */

function UsePipe(token, options = {}) {
  return ParamFn(param => {
    param.store.set(token, options);
    param.pipes.push(token);
  });
}

/**
 * Use JsonMapper to deserialize the data.
 *
 * @decorator
 * @operation
 * @input
 * @pipe
 */

function UseDeserialization(options = {}) {
  return UsePipe(DeserializerPipe, options);
}

/**
 * Get data from a path inside the given object
 *
 * @param expression
 * @decorator
 * @operation
 * @input
 * @pipe
 */

function UseParamExpression(expression) {
  return ParamFn(param => {
    param.expression = expression || "";
  });
}

/**
 * Get the object from request (like body, params, query, etc...).
 *
 * @decorator
 * @operation
 * @input
 * @pipe
 */

function UseParamType(options) {
  return useDecorators(ParamFn(param => {
    if (isParameterType(options.paramType)) {
      param.parameter.in(options.paramType);
    }

    param.paramType = options.paramType;
    param.dataPath = options.dataPath;
  }), UseParamExpression(options.expression), options.expression && Name(options.expression));
}

/**
 * Set the type of the item colllection.
 *
 * Prefer @@CollectionOf@@ usage.
 *
 * @param type
 * @decorator
 * @operation
 * @input
 * @alias CollectionOf
 */

function UseType(type) {
  return CollectionOf(type);
}

/**
 * Enable validation on the decoratored parameter.
 *
 * @decorator
 * @operation
 * @input
 * @pipe
 */

function UseValidation() {
  return ParamFn(param => {
    if (param.type || param.collectionType) {
      param.pipes.push(ValidationPipe);
    }
  });
}

const _excluded = ["paramType", "useType", "expression", "useValidation", "useConverter"];
/**
 * @ignore
 * @param options
 */

function mapPipes(options) {
  const {
    useType,
    useValidation,
    useConverter
  } = options,
        props = _objectWithoutPropertiesLoose(options, _excluded);

  return [useType ? UseType(useType) : ParamFn((entity, parameters) => {
    if (entity.isCollection && entity.type === Object) {
      Any()(...parameters);
    }
  }), useValidation && UseValidation(), useConverter && UseDeserialization(props)];
}

function UseParam(...args) {
  const options = _extends({
    dataPath: "$ctx"
  }, (isString(args[0]) ? args[1] : args[0]) || {}, {
    paramType: isString(args[0]) ? args[0] : args[0].paramType
  });

  return useDecorators(UseParamType(options), ...mapPipes(options));
}

function Context(...args) {
  const {
    expression,
    useType,
    useConverter = false,
    useValidation = false
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.$CTX,
    dataPath: "$ctx",
    expression,
    useType,
    useConverter,
    useValidation
  });
}

function BodyParams(...args) {
  const {
    expression,
    useType,
    useConverter = true,
    useValidation = true
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.BODY,
    dataPath: "$ctx.request.body",
    expression,
    useType,
    useConverter,
    useValidation
  });
}
/**
 * RawBodyParams return the value from [request.body](http://expressjs.com/en/4x/api.html#req.body) as a Buffer.
 *
 * #### Example
 *
 * ```typescript
 * @Controller('/')
 * class MyCtrl {
 *    @Post('/')
 *    create(@RawBodyParams() body: Buffer) {
 *       console.log('Entire body', body.toString('utf8'));
 *    }
 * }
 * ```
 *
 * @decorator
 * @operation
 * @input
 */

function RawBodyParams() {
  return useDecorators(UseParam({
    paramType: ParamTypes.RAW_BODY,
    dataPath: "$ctx.request.rawBody",
    useType: Object,
    useConverter: false,
    useValidation: false
  }));
}

function HeaderParams(...args) {
  const {
    expression,
    useType,
    useConverter = false,
    useValidation = false
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.HEADER,
    dataPath: "$ctx.request.headers",
    expression,
    useType,
    useConverter,
    useValidation
  });
}

function CookiesParams(...args) {
  const {
    expression,
    useType,
    useConverter = false,
    useValidation = false
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.COOKIES,
    dataPath: "$ctx.request.cookies",
    expression,
    useType,
    useConverter,
    useValidation
  });
}
function Cookies(...args) {
  // @ts-ignore
  return CookiesParams(...args);
}

function Locals(...args) {
  const {
    expression,
    useType,
    useConverter = false,
    useValidation = false
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.LOCALS,
    dataPath: "$ctx.response.locals",
    expression,
    useType,
    useConverter,
    useValidation
  });
}

function PathParams(...args) {
  const {
    expression,
    useType,
    useConverter = true,
    useValidation = true
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.PATH,
    dataPath: "$ctx.request.params",
    expression,
    useType,
    useConverter,
    useValidation
  });
}
/**
 * RawPathParams return the raw value from [request.params](http://expressjs.com/en/4x/api.html#req.params) object.
 *
 * Any validation and transformation are performed on the value. Use [pipes](/docs/pipes.html) to validate and/or transform the value.
 *
 * #### Example
 *
 * ```typescript
 * @Controller('/')
 * class MyCtrl {
 *    @Get('/')
 *    get(@RawPathParams() params: string) {
 *       console.log('Entire params', params);
 *    }
 *
 *    @Get('/')
 *    get(@RawPathParams('id') id: string) {
 *       console.log('ID', id);
 *    }
 * }
 * ```
 * > For more information on deserialization see [converters](/docs/converters.md) page.
 *
 * @param expression The path of the property to get.
 * @decorator
 * @operation
 * @input
 */

function RawPathParams(expression) {
  return UseParam({
    paramType: ParamTypes.PATH,
    dataPath: "$ctx.request.params",
    expression
  });
}

function QueryParams(...args) {
  const {
    expression,
    useType,
    useConverter = true,
    useValidation = true
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.QUERY,
    dataPath: "$ctx.request.query",
    expression,
    useType,
    useConverter,
    useValidation
  });
}
/**
 * RawQueryParams return the value from [request.query](http://expressjs.com/en/4x/api.html#req.query) object.
 *
 * Any validation and transformation are performed on the value. Use [pipes](/docs/pipes.html) to validate and/or transform the value.
 *
 * #### Example
 *
 * ```typescript
 * @Controller('/')
 * class MyCtrl {
 *    @Get('/')
 *    get(@RawPathParams() params: any) {
 *       console.log('Entire params', params);
 *    }
 *
 *    @Get('/')
 *    get(@RawPathParams('id') id: string) {
 *       console.log('ID', id);
 *    }
 * }
 * ```
 * > For more information on deserialization see [converters](/docs/converters.md) page.
 *
 * @param expression The path of the property to get.
 * @decorator
 * @operation
 * @input
 */

function RawQueryParams(expression) {
  return UseParam({
    paramType: ParamTypes.QUERY,
    dataPath: "$ctx.request.query",
    expression
  });
}

function Session(...args) {
  const {
    expression,
    useType,
    useConverter = false,
    useValidation = false
  } = mapParamsOptions(args);
  return UseParam({
    paramType: ParamTypes.SESSION,
    dataPath: "$ctx.request.session",
    expression,
    useType,
    useConverter,
    useValidation
  });
}

export { BodyParams, Context, Cookies, CookiesParams, DeserializerPipe, HeaderParams, Locals, ParamFn, ParamMetadata, ParamTypes, ParamValidationError, ParseExpressionPipe, PathParams, PlatformParams, QueryParams, RawBodyParams, RawPathParams, RawQueryParams, RequiredValidationError, Session, UseDeserialization, UseParam, UseParamExpression, UseParamType, UsePipe, UseType, UseValidation, ValidationError, ValidationPipe, mapParamsOptions };
//# sourceMappingURL=index.modern.js.map
